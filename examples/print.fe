main : int (argc: int, argv: string[]) => {
	a : int = 2;
	put("Argc:");
	show(argc);
	other(a, argc);
	return 0; // optional
}

other : int (a : int, b : int) => {
	put("a:");
	show(a);
	put("b:");
	show(b);
}
