main : int (argc: int, argv: string) => {
	a : int = 2;
	b : int = 341_334;
	put("Argc:");
	show(argc);
	other(a, b);
	return a; // optional
}

other : int (a : int, b : int) => {
	put("a:");
	show(a);
	put("b:");
	show(b);
}
