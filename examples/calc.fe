main : int () => {

	a : int = 2;
	b : int = 3;
	c : int = 1;

	a = a & b;
	b = 4;
	b = square(square(b));
	c = square(3) % 4;
	
	show(a);
	show(b);
	show(c);
}

square: int (num: int) => {
	return num * num;
}
